info:
  name: /ledgers/{ledgerId}/accounts/{accountId}
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/ledgers/{{ledgerId}}/accounts/{{accountId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
