info:
  name: /ledgers/{ledgerId}
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/ledgers/{{ledgerId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
